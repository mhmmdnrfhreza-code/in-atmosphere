export function isHeavyRain(rain: number): boolean {
  return rain >= 10;
}

export function isStrongWindGust(windGust: number): boolean {
  return windGust >= 45;
}

export function isHighRainProbability(probability: number): boolean {
  return probability >= 85;
}